//An example of how to do dropdown menus

/*$("#button-container button").on("click", function(event) {
  if (isHidden) {
    hiddenBox.show();
    isHidden=false;
  }else{
    hiddenBox.hide();
    isHidden=true;
  }
});*/